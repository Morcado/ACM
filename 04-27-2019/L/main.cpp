#include <bits/stdc++.h>

using namespace std;

int main() {
    int cont[5] = {1};
    string a[5];

    cin >> a >> b >> c >> d;

    for (int i = 0; i < 5; i++) {
        for (int j = i + 1; i < 5; j++) {
            if (a[i][0] == a[j][0] {
                cont1 ++;
            })
        }
    }

    cout << resp
    return 0;
}