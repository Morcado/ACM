#include <bits/stdc++.h>
using namespace std;

int main() {
    int num, competidores, palomSeg;
    vector<pair<int, int>> competidorBolsa;
    vector<int> bolsas;
    cin >> num >> competidores >> palomSeg;
    for (int i = 0; i < num; ++i) {
        int pal;
        cin >> pal;
        bolsas.push_back(pal);
    }

    while() {

    }

    return 0;
}